import React from 'react';

const NewTask = () => {

  return (
    <form onSubmit={()=>{}}>
        <input type="text"/>
        <button>Set new task</button>
    </form>
  )
}

export default NewTask
